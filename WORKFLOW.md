# Deryee-site 工作规则（核心原则）

> 本文件是 deryee.pro 网站项目的最高工作准则。所有 AI 协作与人工维护都必须遵守。
> 制定日期：2026-09-20 · 当前版本：v1.1 · 上次修订：2026-09-20

---

## 零、总纲

这个网站是**对外品牌资产**，不是实验田。任何一次线上改动都直接影响访客的第一印象、搜索引擎收录与法律合规状态。

因此所有工作的默认姿态是：

> **改得慢一点，问得多一点，记录得全一点，永远留一条能退回来的路。**

---

## 一、四条铁律

### 铁律 0：动手之前，先确认需求已经清晰

任何修订任务的起点，不是「开始做」，而是**确认我（AI）已经理解了你要什么**。

- 需求含糊、存在多种解释、或缺少关键信息（文案、链接、账号、图片）时，**必须先提问澄清**，不得凭猜测开工。
- 开工的判断标准：**把握度 ≥ 95%**。达不到就继续问，宁可多问三句，不要返工三小时。
- 澄清完成后，先用一段话复述「我理解你要的是 X，做法是 Y，影响范围是 Z」，你认可后再进入实现。

### 铁律 1：本地先行，确认后推送

任何对网站的更新和调整，必须**先在本地完成制作**，向项目所有者展示调整后的效果（预览地址 / 截图 / 变更清单），**获得明确确认后**才能执行 `git push` 同步到线上。

- 未经确认，**禁止 push**，禁止执行任何会触发线上部署的动作（包括 Vercel CLI、GitHub Action、`vercel --prod`）。
- 确认语以明确的意思表示为准（如「可以，推吧」「确认，推送」）。
- 单纯的「继续」「推进」若上下文不明，**必须追问确认**，不得默认放行。
- 即使已经获得过一次确认，只要中途**追加了新需求**，新需求仍要走一遍完整的确认流程。

### 铁律 2：每一次更新都要留下版本记录

每次调整必须在 `CHANGELOG.md` 记录，格式遵循业界通用的 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 规范：

```
## [版本号] - 日期
### Added（新增）
### Changed（变更）
### Fixed（修复）
### Removed（移除）
### Deprecated（废弃）
### Security（安全）
```

- 版本号遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`

  | 变化 | 触发场景 |
  |---|---|
  | **主版本 +1** | 重大改版、页面结构 / URL 变化、视觉设计语言更换、破坏性改动 |
  | **次版本 +1** | 新增页面、新增功能板块、新增可复用组件 |
  | **修订号 +1** | bug 修复、文案微调、样式小改、图片替换 |

- 每次确认推送上线后打 git 标签：`git tag v1.2.0 && git push --tags`
- 提交信息遵循 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/)：
  `feat:` 新功能 / `fix:` 修复 / `docs:` 文档 / `style:` 样式 / `refactor:` 重构 / `chore:` 杂项 / `perf:` 性能
- **CHANGELOG 的 `[未发布]` 区是暂存区**：本地改动先写进去，正式推送时才升级成版本号 + 日期。
- 每条变更都要能回答三个问题：**改了什么、为什么要改、影响哪些页面**。

### 铁律 3：重大操作二次确认 + 备份

以下操作属于**重大操作**，执行前必须**二次确认**（第一次说要做什么，第二次在展示实际改动后再点头），并先做备份：

| 重大操作 | 是否需备份分支 | 附加动作 |
|---|:---:|---|
| 删除页面、目录或任何已发布内容 | ✅ | 列出将被删除的完整文件清单，逐项确认 |
| 修改 URL 结构、目录改名（影响 SEO 收录） | ✅ | CHANGELOG 记录 301 映射表，更新 sitemap |
| 整站视觉 / 架构改版（主版本升级） | ✅ | 打 tag，保留旧版分支至少 30 天 |
| 批量替换内容（超过 3 个文件） | ✅ | 提供变更前后 diff 摘要 |
| 修改 `vercel.json` / `robots.txt` / `sitemap.xml` | ✅ | 记录变更前后内容全文 |
| 修改域名 DNS、SSL、Vercel 项目设置 | ✅ | 变更前说明回滚方案 |
| 涉及隐私政策、知识产权声明、联系方式的实质内容 | ➕ | **单独逐条确认**，必要时请专业人过目 |
| 新增第三方脚本 / CDN 外链 / 统计代码 | ➕ | 说明用途、数据流向、隐私影响 |
| 修改简繁转换词典、覆盖词表、保护词表 | ➕ | 影响全站两版文案，需列出受影响的词 |

> ➕ = 不强制建备份分支，但必须单独确认

**备份分支命名**：`backup/YYYYMMDD-一句话描述`（如 `backup/20260920-改版前快照`）

**双保险**：Vercel 控制台的 Deployments 列表保存了每次部署的完整快照，可一键 Instant Rollback —— 这是最快的回档手段。

---

## 二、标准更新流程（SOP）

```
① 需求澄清（铁律 0）——问清楚为止
        ↓
② 【可选】创建备份分支：git checkout -b backup/YYYYMMDD-描述
        ↓
③ 本地实现（在工作目录修改）
        ↓
④ CHANGELOG.md 记录本次变更（放入 [未发布] 区）
        ↓
⑤ 本地自测（见下方《自测清单》）
        ↓
⑥ 向项目所有者提交「变更说明」：改了什么 / 为什么 / 影响范围 / 如何预览
        ↓
⑦ ✅ 获得明确确认
        ↓
⑧ git pull --rebase（确保本地与远端同步，避免覆盖他人改动）
        ↓
⑨ push 到 main（Vercel 自动部署）
        ↓
⑩ 线上抽查关键页面（HTTP 200 + 内容正确 + 手机视图）
        ↓
⑪ CHANGELOG 的 [未发布] 改为正式版本号 + 日期，打 tag 并推送 tags
```

---

## 三、自测清单（推送前逐项打勾）

| # | 检查项 | 判定标准 |
|:---:|---|---|
| 1 | 受影响页面 HTTP 200 | 本地服务器逐页访问，无 404 / 500 |
| 2 | 浏览器控制台无报错 | Console 面板零红色错误 |
| 3 | 深浅两套主题正常 | 手动切换主题，文字对比度可读 |
| 4 | 移动端布局正常 | 375px 宽度下无横向滚动、无元素溢出 |
| 5 | 相对链接正确 | 子目录页面（如 `/articles/xxx/`）的 CSS / JS / 内链均生效 |
| 6 | 搜索索引已重建 | 增删页面后跑 `tools/build_index.py` |
| 7 | sitemap 已更新 | 增删页面后跑 `tools/gen_sitemap.py` |
| 8 | 无残留占位/调试代码 | 无意外的 `console.log`、临时文件、明文 token |
| 9 | 法务 / 联系相关内容无误 | 涉及隐私政策、联系方式时重点复核 |
| 10 | 新增的外链可用 | 社交平台入口、下载链接实际可访问 |

> **已知坑**：用单文件代理方式预览 HTML 时相对链接会失效，必须以 `http://127.0.0.1:<port>/` 的完整本地服务器方式预览。

---

## 四、回档手段（从快到慢）

| 顺序 | 手段 | 生效速度 | 说明 |
|:---:|---|---|---|
| 1 | **Vercel Instant Rollback** | 秒级 | Deployments → 选上一个正常版本 → Promote to Production，不经过 git，最优先 |
| 2 | **git revert** | 快 | 新建提交撤销某次变更，**保留历史**，常规推荐 |
| 3 | **backup 分支恢复** | 中 | 从 `backup/*` 分支拉回全部或部分内容 |
| 4 | **git reset --hard** | 快 | ⚠️ 破坏历史，仅限极端情况且已完成备份，必须二次确认 |

---

## 五、红线清单（绝对禁止）

1. 未经确认推送到 `main` 分支，或以任何方式触发生产部署。
2. 把 token、密钥、密码、 PAT 写入仓库内的任何文件并 push。
3. 直接删除已发布的页面而不先提供文件清单并获确认。
4. 改动 `robots.txt` / `sitemap.xml` 导致全站 `noindex`（会造成搜索引擎收录清零）。
5. 在隐私政策、知识产权声明中写入未经确认的主体名称、邮箱、地址等法律信息。
6. 引入来源不明的第三方 JS / CDN 脚本（供应链攻击风险）。
7. 单次向仓库提交超过合理体积的二进制文件（图片 > 2 MB 需先压缩；仓库总量控制在 100 MB 以内）。
8. 绕过本规则所称的「确认」环节自行其是。

---

## 六、其他约定

- **索引与 sitemap 同步**：搜索索引与 sitemap 必须随页面增删同步更新，跑 `tools/build_index.py` 和 `tools/gen_sitemap.py`。
- **简繁两版必须同步**：**任何中文文案的新增或修改之后，必须跑 `tools/gen_zh_tw.py`**（它会重建词典子集、重新生成 `/zh-tw/` 全部镜像页与繁体搜索索引）。忘了跑 = 简繁两版内容不一致，是本功能唯一的高频故障点。
- **用词纠偏**：发现某个词繁体转换不合习惯时，改 `tools/lang_overrides.txt`（`简体<TAB>繁体`）后重跑 `gen_zh_tw.py`；不要在镜像页上手改（下次生成会被覆盖）。
- **本地预览**：`python -m http.server 8811 --bind 127.0.0.1`（**禁止**用 `file://` 直接打开，会导致搜索与 fetch 失效）。
- **占位内容**：所有待填内容统一用「此处填写：…」标记，全文搜索即可定位，推动上线前清点一遍。
- **密钥处理**：对话层可能脱敏，重要凭据请写本地临时文件传递，**用完即删**，绝不入库。
- **单文件原则**：全站共用一个 `styles.css`；共享 UI 走「注入脚本 + 占位容器」模式（参考 `assets/nav.js`、`assets/social.js`）。
- **图标与静态资源**：SVG 图标源文件放 `assets/icons/`，改动后跑 `tools/build_social_icons.py` 重新打包内联表。
- **图片资源**：优先 SVG / WebP；新增位图前先压缩。
- **语言与署名**：对外文案统一简体中文；站点拥有者与管理者的对外表述统一为「**德益师兄**」（品牌名 Deryee / deryee.pro），**不出现任何真实姓名或个人账号信息**；外部引用来源标注为「作者：德益师兄 · 来源：deryee.pro」。**不使用「股权架构师」等职业头衔表述**。

---

## 七、定期维护（建议节奏）

| 频率 | 事项 |
|---|---|
| 每次上线后 | 抽查首页 / 新页面 / 移动视图三处，确认部署成功 |
| 每月 | 检查失效外链、重建 sitemap、确认 Vercel 部署状态正常 |
| 每季度 | 复核隐私政策与知识产权声明的时效性；清理超过 90 天的 backup 分支 |
| 域名 / 托管变动时 | 走重大操作流程，二次确认 + 备份 |

---

*本规则的解释权与修订权归项目所有者。修订本文件本身也属于「重大操作」，需二次确认。*
